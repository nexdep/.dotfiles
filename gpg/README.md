# GPG backup

This folder contains:

- `public.asc`: public GPG key
- `private.asc.gpg`: encrypted private GPG key backup
- `ownertrust.txt`: GPG trust backup
- `install.sh`: import script

To restore:

```bash
./install.sh
```
